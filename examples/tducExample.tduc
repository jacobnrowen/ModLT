#Recipes { faithful() }
Signature {  
    vocalic(_) high(_) low(_) front(_) round(_) consanantal(_)
    *(_,_) <(_,_)
}

Formulas {
    a(x) := vocalic(x) && low(x) && ~round(x)
}

DOM := \E(x)[vocalic(x)]
CSET := 1

LIC(x) := true

high(v) := high(v) || a(v)
low(v) := low(v) && round(v)
front(v) := high(v) || a(v)
round(v) := false



